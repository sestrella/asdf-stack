#!/bin/bash
get_tags() {
  curl -s https://api.github.com/repos/commercialhaskell/stack/tags
}

filter_versions() {
 grep -oE '[[:digit:]](\.[[:digit:]]+){2}(-[[:alnum:]]+|\.[[:digit:]]+)?'
}

join_lines() {
  tr '\n' ' '
}

get_tags | filter_versions | uniq | join_lines
