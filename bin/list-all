#!/bin/bash
main() {
  versions=$(get_tags | jq -r .[].name | sed "s/^v//" | tac)
  echo $versions
}

get_tags() {
  curl -s "https://api.github.com/repos/commercialhaskell/stack/tags"
}

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]
then
  main
fi
